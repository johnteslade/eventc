Import('env')

env.Append(CCFLAGS = '-g -Wall')

env.Append(LIBS = [
	'rt', # For mq
	'pthread',
])

source_files = [
	"myprog.c",
	"eventc_connections.c",
	"Thread1.c",
	"Thread1_autogen.c",
	"Thread2.c",
	"Thread2_autogen.c",
]

env.Program(
	target = "eventc", 
	source = source_files
)
